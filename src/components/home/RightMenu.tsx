"use client";

import type { FC } from "react";
import React, { memo } from "react";

const RightMenu: FC = () => {
  return (
    <section>
      <h1>RightMenu</h1>
    </section>
  );
};

export default memo(RightMenu);
